services:
  geas:
    build: .
    image: geas:dev
    env_file: apps/blueprint/.env
    stdin_open: true
    tty: true
    volumes:
      - ./apps/blueprint/skills:/app/apps/blueprint/skills:ro
      - geas-data:/root/.geas

volumes:
  geas-data:
